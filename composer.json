{
    "name": "zakaryya/auth",
    "autoload": {
        "files": ["src/z/helpers.php"]
    },

    "scripts": {
        "serve" : [
            "php -S localhost:8000 -d display_errors=1 -t public/"
        ]
    },

    "config": {
        "process-timeout": 0
    },
    "require-dev": {
        "symfony/var-dumper": "^6.1"
    }
}
